// Intentionally empty. model-viewer's CachingGLTFLoader.setMeshoptDecoderLocation()
// requires a script URL that loads successfully as a trigger; the actual
// MeshoptDecoder implementation is already bundled via the static ESM import
// in @google/model-viewer itself (three/examples/jsm/libs/meshopt_decoder.module.js).
